<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@!section('title') SpectrumPro | Premium Eyewear Marketplace @end</title>

    {{-- Tailwind CSS CDN (for development, consider using Tailwind CLI for production) --}}
    <script src="https://cdn.tailwindcss.com"></script>

    {{-- Google Fonts --}}
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">

    {{-- Tailwind Config --}}
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'poppins': ['Poppins', 'sans-serif']
                    }
                }
            }
        }
    </script>

    @!section('styles')
</head>

<body class="font-poppins bg-gray-50 text-gray-600">
    {{-- Header / Navigation --}}
    <header class="bg-white text-gray-800 py-5 sticky top-0 z-50 border-b border-gray-200 shadow-sm backdrop-blur-lg">
        <nav class="flex items-center justify-between max-w-[1400px] mx-auto px-8">
            <a href="{{ route('home') }}"
                class="flex items-center text-2xl font-extrabold tracking-tight text-gray-800 ml-0 md:ml-8">
                <div
                    class="w-8 h-8 mr-2 bg-gradient-to-br from-gray-800 to-green-400 rounded-lg flex items-center justify-center relative before:content-[''] before:w-5 before:h-3 before:border-2 before:border-white before:rounded-xl before:absolute after:content-[''] after:w-2 after:h-0.5 after:bg-white after:absolute after:top-1/2 after:left-1/2 after:-translate-x-1/2 after:-translate-y-1/2">
                </div>
                SpectrumPro
            </a>

            <div class="nav-links hidden md:flex gap-8">
                <a href="{{ route('home') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">Home</a>
                <a href="{{ route('products.index') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">Shop</a>
                <a href="{{ route('about') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">About</a>
                <a href="{{ route('contact.show') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">Contact</a>

                @if(auth.isAuthenticated)
                @if(auth.user.role === 'seller')
                <a href="{{ route('seller.products') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">My
                    Products</a>
                <a href="{{ route('seller.wishlists') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">Wishlists</a>
                @else
                <a href="{{ route('user.wishlist') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">My
                    Wishlist</a>
                @endif
                <form action="{{ route('auth.logout') }}" method="POST" class="inline">
                    {{ csrfField() }}
                    <button type="submit"
                        class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-red-600 hover:bg-red-50 hover:-translate-y-0.5">Logout</button>
                </form>
                @else
                <a href="{{ route('auth.login.show') }}"
                    class="text-gray-700 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:text-green-600 hover:bg-green-50 hover:-translate-y-0.5">Login</a>
                <a href="{{ route('auth.register.show') }}"
                    class="text-white bg-green-600 font-medium text-sm px-5 py-3 rounded-lg transition-all duration-300 hover:bg-green-700 hover:-translate-y-0.5">Register</a>
                @endif
            </div>

            <div class="hamburger flex md:hidden flex-col cursor-pointer mr-4" tabindex="0" aria-label="Open menu">
                <span class="h-0.5 w-6 bg-gray-800 my-0.5 rounded-sm transition-all duration-200"></span>
                <span class="h-0.5 w-6 bg-gray-800 my-0.5 rounded-sm transition-all duration-200"></span>
                <span class="h-0.5 w-6 bg-gray-800 my-0.5 rounded-sm transition-all duration-200"></span>
            </div>
        </nav>
    </header>

    {{-- Flash Messages --}}
    @if(flashMessages.has('success'))
    <div class="max-w-6xl mx-auto mt-4 px-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
            <span class="block sm:inline">{{ flashMessages.get('success') }}</span>
        </div>
    </div>
    @endif

    @if(flashMessages.has('error'))
    <div class="max-w-6xl mx-auto mt-4 px-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
            <span class="block sm:inline">{{ flashMessages.get('error') }}</span>
        </div>
    </div>
    @endif

    {{-- Main Content --}}
    @!section('content')

    {{-- Footer --}}
    <footer class="bg-white text-gray-600 py-12 px-4 text-center mt-16 border-t border-gray-200">
        <div class="flex justify-center gap-8 mb-4 flex-wrap">
            <a href="{{ route('home') }}"
                class="text-gray-600 font-medium transition-colors duration-200 hover:text-gray-800">Home</a>
            <a href="{{ route('products.index') }}"
                class="text-gray-600 font-medium transition-colors duration-200 hover:text-gray-800">Shop</a>
            <a href="{{ route('about') }}"
                class="text-gray-600 font-medium transition-colors duration-200 hover:text-gray-800">About</a>
            <a href="{{ route('contact.show') }}"
                class="text-gray-600 font-medium transition-colors duration-200 hover:text-gray-800">Contact</a>
        </div>

        <div class="text-sm text-gray-400 mt-6">&copy; 2025 SpectrumPro. All rights reserved.</div>
    </footer>

    {{-- JavaScript --}}
    <script src="/js/script.js"></script>
    @!section('scripts')
</body>

</html>