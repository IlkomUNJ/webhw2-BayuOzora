@layout('layouts/main')

@section('title')
  Edit Product | SpectrumPro
@end

@section('content')
<section class="max-w-6xl mx-auto my-12 px-4">
  <h2 class="text-4xl font-semibold text-gray-800 mb-8 text-center tracking-tight">Edit Product</h2>
  <form action="{{ route('seller.update', { id: product.id }) }}" method="POST" enctype="multipart/form-data" class="bg-white rounded-2xl shadow-lg p-8 max-w-2xl mx-auto">
    {{ csrfField() }}
    
    <label for="title" class="block mb-2 text-gray-800 font-medium">Product Title</label>
    <input type="text" id="title" name="title" value="{{ product.title }}" required class="w-full px-4 py-3 mb-4 border border-gray-300 rounded-lg text-base font-poppins focus:outline-none focus:border-green-600">
    
    <label for="description" class="block mb-2 text-gray-800 font-medium">Description</label>
    <textarea id="description" name="description" rows="5" required class="w-full px-4 py-3 mb-4 border border-gray-300 rounded-lg text-base font-poppins focus:outline-none focus:border-green-600">{{ product.description }}</textarea>
    
    <label for="price" class="block mb-2 text-gray-800 font-medium">Price ($)</label>
    <input type="number" id="price" name="price" value="{{ product.price }}" step="0.01" required class="w-full px-4 py-3 mb-4 border border-gray-300 rounded-lg text-base font-poppins focus:outline-none focus:border-green-600">
    
    <div class="mb-4">
      <label class="block mb-2 text-gray-800 font-medium">Current Image</label>
      <img src="{{ product.image }}" alt="{{ product.title }}" class="w-48 h-48 object-contain bg-gray-50 rounded-lg p-4 border border-gray-200">
    </div>
    
    <label for="image" class="block mb-2 text-gray-800 font-medium">Update Image URL</label>
    <input type="text" id="image" name="image" value="{{ product.image }}" placeholder="https://example.com/image.jpg" class="w-full px-4 py-3 mb-2 border border-gray-300 rounded-lg text-base font-poppins focus:outline-none focus:border-green-600">
    <p class="text-sm text-gray-500 mb-4">Or upload a new image file:</p>
    <input type="file" name="image" accept="image/*" class="w-full px-4 py-3 mb-6 border border-gray-300 rounded-lg text-base font-poppins focus:outline-none focus:border-green-600">
    
    <div class="flex gap-4">
      <button type="submit" class="flex-1 bg-green-600 text-white border-0 px-8 py-3 text-base rounded-lg cursor-pointer font-bold shadow-md transition-colors duration-200 hover:bg-green-700">Update Product</button>
      <a href="{{ route('seller.products') }}" class="flex-1 bg-gray-200 text-gray-800 border-0 px-8 py-3 text-base rounded-lg cursor-pointer font-bold shadow-md transition-colors duration-200 hover:bg-gray-300 text-center">Cancel</a>
    </div>
  </form>
</section>
@end
